{
  "name": "{{ packageName }}",
  "version": "1.0.0",
  "main": "./src/index.js",
  "scripts": {
    "forge:build": "forge build",
    "forge:test": "forge test",
    "forge:deploy": "forge script script/Deploy.s.sol:DeployScript --rpc-url ${RPC_URL} --broadcast --verify"
  }
}
